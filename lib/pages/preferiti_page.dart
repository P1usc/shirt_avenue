import 'package:flutter/material.dart';

class PreferitiPage extends StatelessWidget {
  const PreferitiPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Pagina Preferiti'));
  }
}
